language: php

php:
    - 7.1
    - 7.2
    - 7.3
    - nightly

matrix:
    fast_finish: true
    allow_failures:
        - php: nightly

before_install:
    - travis_retry composer self-update

install:
    - travis_retry composer install -n --prefer-dist

script:
    - php vendor/bin/phpunit
